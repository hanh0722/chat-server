import { Document, QueryOptions } from "mongoose";
import User from "../../model/User";

export const sortByQuery = async <T extends QueryOptions>(model: T) => {
  try{
    
  }catch(err) {
    return null;
  }
}